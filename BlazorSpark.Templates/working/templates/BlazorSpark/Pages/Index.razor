@page "/"
@inject ILogger logger
<section class="max-w-5xl mx-auto h-screen flex justify-center items-center px-4">
    <article>
        <h1 class="text-4xl font-bold text-gray-800 text-center">
            Build something beautiful
        </h1>
        <p class="text-lg text-center mt-6 max-w-xl mx-auto">
            Blazor Spark picks up where .NET Blazor leaves off. Build production ready, full-stack web applications fast.
        </p>
        <p class="text-lg text-center mt-6">
            View the <a class="text-purple-600 font-semibold" href="https://blazorspark.com" target="_blank">documentation →</a>
        </p>
    </article>
</section>
@code {

    protected override void OnInitialized()
    {
        logger.Information($"Initialized at {DateTime.Now}");
    }
}